#ifndef EXTRAHEADER_PROBE_H
#define EXTRAHEADER_PROBE_H

/* Marker pulled in only when $C.extraheader is honored. */
#define EXTRAHEADER_PROBE_INCLUDED 1

/* Prefix applied by $C.extendedattribute; empty so generated decls stay valid. */
#ifndef EXTRAHEADER_EXTATTR
#define EXTRAHEADER_EXTATTR
#endif

#endif
